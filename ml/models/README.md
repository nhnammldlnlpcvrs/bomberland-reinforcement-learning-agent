# Model Artifacts

This directory is reserved for future small model artifacts and metadata.

Do not commit large checkpoints. Keep `.pth`, `.onnx`, replay buffers, and generated datasets out of git unless there is a clear lightweight fixture need.

